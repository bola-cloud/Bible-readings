package com.example.biblereadings2

// This file used to contain an adapter referencing HomeCard/title/icon which don't exist anymore.
// The project uses `HomeAdapter.kt` instead. Keep this placeholder to avoid compile errors until
// any leftover references are removed.
class HomeGridAdapterPlaceholder {
    // intentionally empty
}
